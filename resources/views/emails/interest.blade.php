<!DOCTYPE html>
<html>
<head>
    <title>Contact Mail</title>
</head>
<body>
    <h1>Hello, {{ $data['fullName'] }}</h1>
    <p>{{ $data['phoneNumber'] }}</p>
    <p>{{ $data['email'] }}</p>
    <p>{{ $data['country'] }}</p>
    <p>{{ $data['agerange'] }}</p>
    <p>{{ $data['course'] }}</p>
    <p>{{ $data['courseDate'] }}</p>
</body>
</html>
